window.onload = function()
            {
                carrega_nome();
            window.document.getElementById('um').onclick = function()
    
            {
                information();
            }
            window.document.getElementByld('um').oncliack = function()
            {
                information();
            }
}

            function information(){
                var name = window.prompt('digite seu nome');
                var idade = window.prompt('digite sua idade');
                window.alert(nome + ' ' + idade);
            }
            function carrega_nome()
            {
                window.document.getElementByld('nome').value = "Alexandre";
            }
